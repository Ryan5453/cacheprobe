[project]
name = "openrouter-timing-attacks"
version = "0.1.0"
description = "Security research: Auditing prompt caching timing attacks in OpenRouter API gateway"
readme = "readme.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.7.1",
    "pyyaml>=6.0.2",
    "scikit-learn>=1.7.2",
    "scipy>=1.15.3",
]

[project.scripts]
audit = "scripts.main:main"
