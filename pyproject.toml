[project]
name = "cacheprobe"
version = "1.0.0.dev1"
description = "Auditing Prompt Cache Isolation in Inference & Gateway APIs"
readme = "readme.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.7.1",
    "pyyaml>=6.0.2",
    "scikit-learn>=1.7.2",
    "scipy>=1.15.3",
]